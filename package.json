{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "heroku-postbuild": "cd ajlange && npm ci && npm run build"
  },
  "dependencies": {
    "react": "^18.3.1"
  },

  "engines": {
    "node": "20.x",
    "npm": "latest"
  }
}
