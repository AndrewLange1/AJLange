{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd ajlange && npm install && npm run build"
  },
  "dependencies": {
    "react": "^18.3.1"
  },

  "engines": {
    "node": "20.x",
    "npm": "latest"
  }
}
